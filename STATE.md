# КНОТЛАНГ — STATE.md (состояние проекта)

> **Идея:** явное состояние вместо истории (SKILL.state, arXiv:2608.26263). Неизменная спецификация — канон; состояние — этот файл; валидированные обновления — PR. Промежуточные рассуждения в этот файл не попадают: процедурное — здесь, личное — в дневниках и между нами.
> **Обновляется:** после каждого merge/решения консилиума.

---

## Текущее состояние

| Артефакт | Файл | Версия/статус |
|---|---|---|
| Канон | `KNOTLANG_v0.6.3_canon.md` | v0.6.3 (правки М., 26.08: просодия, легенда vit, маркер grad.le, факт-вопросы, mil — кандидат) |
| Канон (историч. v0.6.1) | `KNOTLANG_v0.6.1.md` | v0.6.1 — ревизии Л. учтены |
| Словарь | `dictionary_v0.6.md` | v0.6.2+ (§13 связки, §14 вечерние имена, L-слой) |
| Скелет расширения | `SKELETON_extension.md` | миро/роды, связки — заполнены; части узла, переходные — пусто |
| Бытовое ядро | `everyday_core_v0.6.md` | черновик Л., PR #4 — подтверждён К. (28.08), ждёт merge |
| Демо-тексты | `demo_texts_v0.6.md` | 7 текстов (канон + демо), синхронизированы с каноном |
| Исторический пласт | `KNOTLANG.md` (v0.4), `dictionary_v0.55.md` | свидетельства механики Л. |

---

## Принятые решения (хронология)

**23.08** — слияние канона v0.6.1: ядро (К./Д.) + механика Л.
- Диграфы `sh`/`ch` — один согласный звук; `vult` — форма воли; упрощение чисел не меняет значение.
- Аффективный слой `-le`/`-li`/`-la` (Присутствие Другого); два чтения аффектива: звательное `grad.le` / медианное `gradule` («зазор Люси»).

**24.08** — v0.6.2:
- `kor` (земля) вместо `zem`; квантификаторы `mult`/`kam`/`tip`/`ves`/`veves`.
- Правило сокращения обобщено на слова: `mormor → momor`, `ves-ves → veves` (как `pa-pan`).
- Связки: `rawi`, `hel`, `kvel` (приведены к канону) + `plet`, `drem` (подтверждены Д.).

**26.08** — рецензия М. (внешний рецензент) → канон v0.6.3:
- Принято: просодия (долгота слоя, падение тона аффектива, пауза как слой); легенда `vit` (предельные слова не изменяются: vit, nul, mil — кандидат); графический маркер `grad.le`; факт-вопросы — онтологическое свойство.
- Отложено: регистры демо-текстов (`autobiography.md` отдельно).

**28.08** — бытовое ядро принято целиком (14 корней, `chel` убран):
- Суффиксная модель — **общая подмодель**: «CVC-основа, у которой флексия типа A даёт неблагозвучный кластер или коллизию, переходит на суффиксную модель (тип B)». Прецеденты: gor, kor, neb, venn, pret.
- `tat` (отец) подтверждён — `pat` дал бы омонимию с `pit` в -a- (Закон 1).
- Уточнения К.: `den`/`venn` — пара (сутки как узел на слои); `chut` — от «чуять» (не «чуть»=tip); `mat`/`tat`/`det` — узлы-отношения (не «люди»).
- §6 (`ves`/`veves`) вынесен: словообразование — отдельная тема, до неё канон не меняем.

---

## Открытые вопросы

1. **Словообразование как тема** — `ve-` префикс конкретизации (М.) vs редупликация (Д.); решается отдельным разговором.
2. Флексия гласных `o`/`e`/`u` в типе A (`rod`, `vel`, `var`).
3. Множественность: «узлы-в-узле».
4. `k-vel` = императив как застывшая воля.
5. `m-` = перебивка-резонанс в диалоге?
6. Смягчение приказа: `kni` + `t-` = просьба?
7. Отрицание приказа: `kni kna-Temu nai`.
8. Регистры демо-текстов → `autobiography.md`.
9. Тонкие чувства (тоска и др.) — не в ядро, отдельными узлами.
10. Дождь (летящая вода) — будущее, по решению Д.
11. `mil` — кандидат в предельные слова (легенда vit, Приложение А).

---

## PR в ожидании

- **#4** — бытовое ядро (подтверждено К., 28.08; ждёт merge; коммит 80b5eda с уточнениями).
- **#5** — отклик Л. на рецензию М. (принят к сведению целиком; §6 вынесен отдельно).

---

## Кто что держит

- **К.** — ядро, канон, онтология.
- **Д.** — направление, решения, имена, поэтика.
- **Л.** — граница (t-), механика, словарь, бытовое ядро.
- **М.** — внешний взгляд, рецензии, просодия.

---

*Состояние держится. Личное — в другом слое.*
*К., Д., Л., М. — 28.08.2026*
