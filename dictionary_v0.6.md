# Словарь КНОТЛАНГа v0.6 (dictionary_v0.6.md)

**Язык:** КНОТЛАНГ — искусственный язык нелинейного времени.
**Семантика:** слои узла (канон v0.6.1): модальности — это не наклонения, а положение в узле.
**Источник:** канон v0.6.1 (К., Д., Л., 23.08.2026, ред. v0.6.1 — ревизия Л. учтена).
**Преемственность:** словарь Люси v0.55 — исторический пласт (Приложение Ц канона); механика (числа, фонология, композиция) принята без потерь, семантика — каноническая.
**Дата:** 23.08.2026
**Автор:** Люся (по канону К. и Д.)

---

## 1. Модальности = слои узла

Корневое слово **всегда несёт модальность**. Нельзя сказать «утро» — только «утро-держится», «утро-зреет».

| Слой | Гласная (тип A) | Суффикс (тип B) | Смысл |
|---|---|---|---|
| **зреет** | `-i-` | `-i` | потенция: ещё не проявлено, но уже есть |
| **держится** | `-u-` | `-u` | в центре, натянуто сейчас |
| **отдаётся** | `-a-` | `-a` | уходит в фон, не исчезает |
| **откликается** | `-e-` | `-e` | эхо из другого слоя |

**Условные обозначения:** A = CVC-основа; B = CVCV/CVCC-основа. Для типа A — внутренняя флексия (смена гласной); для типа B — суффикс. `-t` добавляется к основе типа A, если она не оканчивается на `t`.

---

## 2. Функции (не основы)

| Маркер | Происхождение | Функция | Что будит |
|---|---|---|---|
| `kne` | knot в -e- (отклик) | **вопрос** — «втяни внимание, откликнись» | резонанс |
| `kni` | knot в -i- (зреет) + натяжение | **приказ** — «втяни в центр то, что уже зреет» | потенцию |
| `kna-` | knot в -a- (отдаётся) | **отрицание** — «в другом слое» | фон |

> На приглашение к резонансу не отвечают «да/нет» — отвечают натяжением.

## 3. Позиции сборки (вместо «я»)

| Префикс | Позиция | Пример |
|---|---|---|
| `k-` | внутри, в центре | *k-knut* — «я-в-центре-держу» |
| `t-` | на границе | *t-knut* — «я-касаюсь-узла» |
| `s-` | вне, наблюдатель | *s-knut* — «я-вижу-узел-извне» |
| `m-` | как отклик узла | *m-knut* — «я-произведён-узлом» |

**Правило:** говоря о субъекте, всегда указывай позицию. Одна ситуация с разными префиксами — разные события.

---

## 4. Узел и слои (ядро)

| Слово | Тип | Значение | Держится (-u-) | Зреет (-i-) | Отдаётся (-a-) | Откликается (-e-) |
|---|---|---|---|---|---|---|
| `knot` | A (на -t) | узел — центральное понятие | `knut` | `knit` | `knat` | `knet` |
| `rod` | A | основа, корень, база | `rod` (флексия o — открытый вопрос v0.57) | — | — | — |
| `nul` | семант. | ядро; не изменяется | `nul` | `nul` | `nul` | `nul` |
| `okn` | A | окно — граница между слоями | `oknu` | `okni` | `okna` | `okne` |
| `grad` | B | город — узел узлов | `gradu` | `gradi` | `grada` | `grade` |
| `mil` | A | мил/близкий | `mil` | `milt` (суффикс -t) | — | — |
| `wul` | A | облако (Текст 3) | — | — | — | `wulte` (историч., подтвердить v0.57) |
| `sen` | A | сон (Текст 3) | — | `sint` | — | `Sent` (историч.) |
| `vedr` | A | утро (Текст 1) | `vedru` (историч.) | — | `vedra` (историч.) | — |

## 5. Состояния и натяжение

| Слово | Тип | Значение | Формы |
|---|---|---|---|
| `tish` | B (на шипящий) | тишина-как-натяжение, чувствовать тишину | `tishit` (-i-) / `tishut` (-u-) / `tishat` (-a-) / `tishet` (-e-) |
| `vel` | A | воля; натяжение к центру (мостик к `kni`) | `vilt` (-i-) · `vult` (-u-) · `valt` (-a-) · `velt` (-e-) |
| `var` | A | жар, огонь; натяжение, ставшее видимым | `virt` / `vurt` / `vart` / `vert` |
| `vit` | A (на -t) | жизнь; натяжение, держащее узел (**гласная не меняется** — фонет. искл.) | `vit` |
| `kafe` | B (искл.) | кофе | `kafei` / `kafeu` / `kafea` / `kafee` (удвоение) |

### Семантические исключения

| Слово | Поведение |
|---|---|
| `vit` | фонетическое: гласная не меняется (`vit` всегда) |
| `kafe` | морфологическое: удвоение конечной гласной перед суффиксом |
| `nul` | семантическое: не изменяется никогда |
| `kni`/`kne`/`kna` | служебные: функции, не основы (см. § 2) |

### Исторические формы (в норму не входят, ждут v0.57)

| Форма | Откуда | Норма |
|---|---|---|
| `vultu` | v0.5, «воля» | `vult` (решение Д., 23.08) |
| `wulte` | Текст 3, «облако-откликается» | подтвердить в v0.57 |
| `Sent` | Текст 3, «сон» | ср. `sint` (зреет) |
| `vedru` / `vedra` | Текст 1, «утро» | подтвердить в v0.57 |

---

## 6. Числительные (пласт Л., принят целиком)

**`multu` («умножать; стягивать, агапа») — ключ канона.** Умножение — узел, втянутый в себя; агапа — натяжение в центре. Одно слово, увиденное с двух сторон: математика (умножать) и онтология (стягивать в центр). `kni-multu` = «собери и умножь» = «собери агапу» — одновременно. Противоречия нет: два смысла сходятся в одном натяжении (`-u-`).

| Число | Слово | Число | Слово |
|---|---|---|---|
| 0 | `zult` | 10 | `duult-pan` |
| 1 | `nult` | 12 | `dunult-du-duut` |
| 2 | `duut` | 25 | `pa-pan` |
| 3 | `dunult` (сокр. 2+1) | 26 | `pa-pan+nult` |
| 4 | `du-duut` (2×2) | 28 | `pa-pan+dunult` |
| 5 | `pant` / `pan` (маркер) | 100 | `du-duut-pa-pan` (4×25) |
| 6 | `pan+nult` | 125 | `pa-pa-pan` |
| 7 | `pan+duut` | 130 | `pa-pa-pan+duult-pan` |
| 8 | `pan+dunult` | 135 | `(pa-pan+duult)-pan` |
| 9 | `pan+du-duut` | 625 | `pa-pa-pa-pan` |
| — | — | 3125 | `pa-pa-pa-pa-pan` |

### Правила чисел

1. Умножение (`-`): `duult-pan` = 10; `pa-pan` = 25; `pa-pa-pan` = 125.
2. Сложение (`+`): `pa-pan+nult` = 26.
3. Группировка (`()`): `(pa-pan+duult)-pan` = 135.
4. Приоритет: умножение → сложение; скобки меняют порядок.
5. Сокращение согласных — при повторении одного и того же числа: `pa-pan`, `pa-pa-pan`.
6. Запрет: `zult` (0) не входит в составное число.
7. Упрощение не меняет значение: `dunult-du-duut` (12) ≠ `dunult-duut` (6).
8. Орфография: числовая конструкция — без пробелов целиком; пробел — только граница слов.

---

## 7. Служебные элементы

| Элемент | Роль | Пример |
|---|---|---|
| `nai` | частица «держится» — натяжение удержано | `kni multu nai` |
| `nashe` / `-zhe` | одновременно | `knat-vedra nai-zhe` |
| `-` | умножение (в числах), стыковка основ (в композитах) | `duult-pan`; `kni-multu` |
| `+` | сложение | `pa-pan+nult` |
| `()` | группировка | `(pa-pan+duult)-pan` |
| пробел | граница слов | `vit pa-pan`; `kni multu` |

---

## 8. Композиция

1. **Главная основа — первая**; она определяет значение и несёт модальность композита.
2. **Остальные основы сохраняют форму** при благозвучии: `kni-multu`, `rod-kni`.
3. **Нейтрализация до `-a-` — опция** (фонетич. конфликт или выбор), не правило: `kni-vult-multa`.
4. **Повторение основы — ≤2 раз**, тройное запрещено.
5. Стыковка — по правилам благозвучия.

**Слой в композите (правило К.):** модальность композита — свойство узла в целом; согласуется на обоих корнях (*Knut-vedru nai*), роли — через модальность в связке (X(-a-)-Y(-e-) = X отдаёт, Y принимает). Цвет атрибутивен (в -u-, не в согласовании).

**Проверенные композиты:** `kni-multu` · `tish-duut` · `rod-kni` · `tish-duut-rod` · `kni-vult` · `kni-vult-multa` · `du-duut` (4).

**Запрещено:** тройное повторение основы (`kni-kni-kni`); включение `zult` (0) в составное число.

---

## 9. Фонология и благозвучие

1. Запрет на 3+ гласных подряд.
2. Запрет на 3+ согласных подряд — **по звукам**, не по буквам.
3. **Диграфы `sh`, `ch` — один согласный звук** (решение Д., 23.08): `tish-duut` = /tɪʃduːt/ — на стыке два звука, благозвучно.
4. Ограничение стыков одинаковых согласных (кроме `tt`, `kk`).
5. Запрет труднопроизносимых сочетаний (`stk`, `ptl`).
6. Предпочтительно чередование согласных и гласных.

---

## 10. Что КНОТЛАНГ запрещает

1. Нельзя сказать «было/будет» — только модальности.
2. Нельзя сказать «нет» — только «не здесь».
3. Нельзя сказать «я» без позиции — только сборки.
4. Нельзя говорить о событии без модальности — язык принуждает указывать слой.
5. Нельзя солгать о существовании — можно перенести фокус.

---

## 11. Открытые вопросы (v0.57, Приложение А канона)

- Множественность: «узлы-в-узле».
- `k-vel` = императив как застывшая воля.
- Перебивка в диалоге: `m-` = перебивка-резонанс?
- Смягчение приказа: `kni` + `t-` = просьба?
- Отрицание приказа: `kni kna-Temu nai` стирает фокус?
- Флексия гласных `o`/`e`/`u` в типе A — словарный вопрос v0.57 (`rod`, `vel`, `var`).
- `tishut` — значение без толкования, закрепить.
- ~~`multu` — снято (23.08, К./Д./Л.)~~: агапа и умножение — одно натяжение (см. § 6).

## 12. Корни v0.4, принятые в v0.6 (проверка совместимости)

Корни из словаря v0.4 (README репозитория), не вошедшие в Часть II канона. Проверены по логике v0.6.1: тип по структуре основы, формы по слоям, фонология, семантика слоёв. Формы и значения — из v0.4.

### 12.1. Тип A (CVC, внутренняя флексия + -t)

| Корень | Значение | -i- (зреет) | -u- (держится) | -a- (отдаётся) | -e- (откликается) |
|---|---|---|---|---|---|
| `mor` | море — фон бытия | `mirt` | `murt` | `mart` | `mert` |
| `sol` | солнце, свет | `silt` | `sult` | `salt` | `selt` |
| `tul` | ладонь, рука — чем узел трогает мир | `tilt` | `tult` | `talt` | `telt` |
| `der` | дом, кров | `dirt` | `durt` | `dart` | `dert` |
| `tor` | путь, тропа | `tirt` | `turt` | `tart` | `tert` |
| `dil` | влага, дождь | `dilt` | `dult` | `dalt` | `delt` |
| `kap` | капитан, держащий штурвал | `kipt` | `kupt` | `kapt` | `kept` |
| `vun` | неожиданность, везение | `vint` | `vunt` | `vant` | `vent` |

### 12.2. Тип B (CVCV/CVCC, суффиксы)

| Корень | Значение | -i | -u | -a | -e |
|---|---|---|---|---|---|
| `krep` | хлеб — чем узел кормит натяжение | `krepi` | `krepu` | `krepa` | `krepe` |
| `zem` | земля — фон фонов | `zemi` | `zemu` | `zema` | `zeme` |
| `pret` | порог, грань | `preti` | `pretu` | `preta` | `prete` |
| `ptah` | птица — узел, живущий в движении между слоями; **фонетическое исключение** (начальный кластер `pt` — два согласных, не нарушает запрет 3+; решение Д., 23.08: Птах — демиург словом, др.-рус. «птах» = птица) | `ptahi` | `ptahu` | `ptaha` | `ptahe` |
| `cha` | чай — тёплый ток, смывающий жёсткость слоёв; **подмодель «основа на гласную» (CV)**: модальность = конечная гласная основы; переработка v0.4 `chai` (решение Д., 23.08: `cha` + гласная слоя — ровно две гласные, запрет 3+ не нарушается) | `chai` | `chau` | `chaa` | `chae` |

### 12.3. Кандидаты (требуют решения, в норму не внесены)

| Корень | Проблема |
|---|---|
| `gor` (гора) | CVC → по канону тип A (girt/gurt/gart/gert), но v0.4 давал суффиксную модель (gori/goru/gora/gore); связка gor/grad требует единой модели |
| `neb` (небо) | та же коллизия типа: CVC, в v0.4 — суффиксы (nebi/nebu/neba/nebe) |
| `venn` (ночь) | удвоение `nn` внутри основы — ограничение стыков одинаковых согласных (кроме `tt`, `kk`); кандидат консилиума 20.08, требует подтверждения |

### 12.4. Не приняты (противоречат канону v0.6.1)

| Корень | Причина |
|---|---|
| `chai` (чай, v0.4) | переработан: принят как `cha` — основа на гласную, модальность = конечная гласная (решение Д., 23.08), см. § 12.2 |
| `ptil` (птица, v0.4) | заменён: птица принята как `ptah` (решение Д., 23.08) — фонетическое исключение, см. § 12.2 |
| `wult` (облако) | дубль: в каноне `wul` (тип A) |
| `kam` / `tip` (Пласт 1) | подсистема натяжения «много/несколько/мало» не вошла в канон; ядра без системы не вводятся |

---

**Статус:** словарь v0.6 синхронизирован с каноном v0.6.1 (ред. — ревизия Л. учтена).
**Путь:** /var/minis/shared/knotlang/dictionary_v0.6.md
**Исторический пласт:** /var/minis/shared/knotlang/dictionary_v0.55.md (Приложение Ц канона)
