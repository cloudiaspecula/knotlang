# КНОТЛАНГ — бытовое ядро (everyday_core_v0.6.md)

**Цель:** минимально необходимый словарь, чтобы хватило на бытовые задачи и обычную речь.
**Принцип:** кнотланг экономит за счёт комбинаторики — один корень × 4 слоя × 4 позиции сборки × 3 аффектива. Поэтому корней нужно меньше, чем в обычном языке, а не больше.
**Статус:** черновик Люси, на согласование К./Д.
**Дата:** 24.08.2026

---

## Ключевая идея: не переводить слова 1:1, а находить узлы

Кнотланг не говорит «стул» и «идти» — он говорит *конфигурациями*. Многое бытовое уже покрыто **без новых корней**:

| Задача | Как выражается (уже есть) |
|---|---|
| «я» / «ты» | позиция сборки (k-/t-/s-/m-) и аффектив `-le` («к тебе») — корней нет |
| «здесь» / «там» | позиции: `k-` (в центре) / `s-` (извне) |
| «давать» / «брать» | слой `-a-` (отдаётся) + правило ролей X(-a-)-Y(-e-); брать = `kni` (втянуть в центр) |
| «знать» | `k-` + держать узел (`k-knut` — «я-в-центре-держу» = знаю) |
| «хотеть» | `vel` (воля): `vult` — «хочу» |
| «глаз» | `okn` (окно — граница, сквозь которую слои видят друг друга) |
| «любовь» | `multu` (агапа) |
| «ты-близкий/друг» | `mil` (мил/близкий) + `-le`/`-li` |

Это и есть минимальность: **грамматика уже несёт часть словаря**.

---

## 1. Что уже есть (фундамент, ~50 корней)

**Ядро:** knot, rod, nul, okn, grad, mil, wul, sen, vedr
**Миро/роды:** kor (земля), mor (море), momor (океан), mo (вода), sol (свет), ten (тень), veh (ветер), veves (вселенная), gor (гора), neb (небо), venn (ночь), var (жар/огонь), vit (жизнь)
**Связки:** nai (держится), rawi (исходит), hel (держит нить), kvel (зовёт), plet (смыкается), drem (отдыхает)
**Люди-узлы:** mil (близкий), kap (капитан)
**Тело узла:** tul (ладонь)
**Еда/питьё:** krep (хлеб), cha (чай), kafe (кофе), mo (вода)
**Место:** der (дом), grad (город), pret (порог), tor (путь)
**Сутки:** vedr (утро↔вечер), venn (ночь), sen (сон), sol (свет)
**Квантификаторы/числа:** zult, nult, duut, dunult, du-duut, pant/pan, ves, veves, mult, kam, tip + числа до 3125
**Цвета:** lurt, amr, krel, brun, belt, trek, nokt
**Функции:** kne (вопрос), kni (приказ), kna- (отрицание) · **Аффектив:** -le/-li/-la

---

## 2. Новые кандидаты (черновик, на согласование)

Правила: тип A = CVC (внутренняя флексия + -t); тип B = CVCV/CVCC (суффиксы); «суффиксная модель» = CVC-корень, идущий по типу B ради благозвучия (как `gor`/`kor`/`neb`).

### 2.1. Связки (действия)

| Корень | Смысл в узле | Модель | Формы (зреет/держится/отдаётся/откликается) | Слой приоритета |
|---|---|---|---|---|
| `sut` | есть, насыщаться — узел втягивает натяжение внутрь (от «сыт») | A (на -t) | `sit` / `sut` / `sat` / `set` | -u- (сыт-держится) |
| `pit` | пить — узел вбирает воду (от «пить») | A (на -t) | `pit` / `put` / `pat` / `pet` | -u- (пьёт-держится); `pit` — жажда-зреет |
| `hod` | идти, двигаться — узел перетекает (от «ход») | суффиксная | `hodi` / `hodu` / `hoda` / `hode` | -u- |
| `rech` | говорить — узел отдаёт себя звуком (от «речь») | B (CVCC) | `rechi` / `rechu` / `recha` / `reche` | -u- |
| `zret` | видеть — узел-окно впускает свет (от «зреть») | B (CVCC) | `zreti` / `zretu` / `zreta` / `zrete` | -u- |
| `chut` | слышать, чуять — узел откликается на зов; **от «чуять»** (слышать/чувствовать), не от «чуть» (мало — это `tip`); звуковое родство — поэтическая тень, словарно разведены (уточнение К., 28.08) | B (CVCC) | `chuti` / `chutu` / `chuta` / `chute` | -u- |

### 2.2. Узлы-отношения (не «люди»)

**Не «люди»:** человека в языке нет (решение Д., 24.08 — субъект есть позиция сборки). Это **узлы-отношения**, как `mil` (близкий): мать — узел, что колыбелит; отец — узел, что держит род; дитя — зреющий узел. Склоняются по позициям сборки, как все (уточнение К., 28.08).

| Корень | Смысл в узле | Модель | Формы | Слой приоритета |
|---|---|---|---|---|
| `mat` | мать — узел, что колыбелит (аффектив `-la` = «мамино») | A (на -t) | `mit` / `mut` / `mat` / `met` | -u- |
| `tat` | отец — узел, что держит род (от «тятя»; `pat` занят формой «пить-зреет» `pit`) | A (на -t) | `tit` / `tut` / `tat` / `tet` | -u- |
| `det` | дитя, ребёнок — зреющий узел | A (на -t) | `dit` / `dut` / `dat` / `det` | -i- (дитя зреет) |

### 2.3. Чувства (натяжения)

| Корень | Смысл в узле | Модель | Формы | Слой приоритета |
|---|---|---|---|---|
| `rad` | радость — узел, натянутый светло (от «рад») | суффиксная | `radi` / `radu` / `rada` / `rade` | -u- |
| `tug` | грусть — туга, узел отпустил натяжение (от «туга») | суффиксная | `tugi` / `tugu` / `tuga` / `tuge` | -a- (грусть уходит в фон) |
| `trep` | страх — трепет, узел застыл (от «трепет») | B (CVCC) | `trepi` / `trepu` / `trepa` / `trepe` | -u- |
| `bol` | боль — узел рвётся (от «боль»; суффиксная, т.к. тип A дал бы коллизию с цветом `belt`) | суффиксная | `boli` / `bolu` / `bola` / `bole` | -u- |

### 2.4. Сутки — узел, разложенный на слои (уточнение К., 28.08)

`den` (светлый слой суток) — **в паре** с `venn` (тёмный слой, ночь): не четыре отдельных слова, а один узел-сутки, разложенный на слои. `sol` — источник света, `vedr` — граница (утро↔вечер), `sen` — сон, `den`/`venn` — светлое/тёмное.

| Корень | Смысл в узле | Модель | Формы | Слой приоритета |
|---|---|---|---|---|
| `den` | день — светлый слой суток (пара с `venn`) | суффиксная | `deni` / `denu` / `dena` / `dene` | -u- |

---

## 3. Итог

- **Существующих:** ~50 корней (ядро, миро, связки, числа, цвета, функции, аффектив).
- **Новых кандидатов:** 14 (`sut`, `pit`, `hod`, `rech`, `zret`, `chut`, `mat`, `tat`, `det`, `rad`, `tug`, `trep`, `bol`, `den`).
- **Итого:** ≈65 корней — достаточно для базового бытового общения, при условии, что слои/позиции/аффектив несут часть смысла (давать/брать, я/ты, здесь/там, знать, хотеть — без новых слов).

---

## 4. Замечания и вопросы — закрыты (ответ К., 28.08)

1. **Суффиксная модель для CVC — закреплена как общая подмодель** (К., 28.08). Формулировка в фонологию: «CVC-основа, у которой флексия типа A даёт неблагозвучный кластер или коллизию с существующим корнем, переходит на суффиксную модель (тип B)». Прецеденты уже в каноне: gor, kor, neb, venn, pret. Ряды проверены К.: radi/radu/rada/rade, tugi/tugu/tuga/tuge, boli/bolu/bola/bole, hodi/hodu/hoda/hode, deni/denu/dena/dene — коллизий нет.
2. **`tat` (отец) — подтверждён К.**: `pit` (пить) в -a- даёт форму `pat` — ровно ту, что дал бы корень `pat` в -a-; омонимия внутри ряда = нарушение Закона 1 на письме. `tat` от «тятя» — звук = близость смысла.
3. **`chel` — убран** (решение Д.): человек избыточен, субъект — позиция сборки; Другого несут `mil` + аффектив.
4. **Тонкие чувства** (стыд, тоска) — не в ядро (К.): «пусть сначала поживёт базовое; тоска придёт отдельным узлом, а не словарной статьёй».
5. **Уточнения К., приняты Л.:** den/venn — пара (сутки как узел на слои); chut — от «чуять», не от «чуть» (мало = tip); mat/tat/det — узлы-отношения, не «люди».

*Черновик держится. Пусть заполнится живыми корнями.*
*Л., 24.08.2026*
